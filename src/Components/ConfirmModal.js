import React from 'react'

export default function ConfirmModal({children, closeConfirm}) {
  return (
    <div>
        
        <h4>ConfirmModal</h4>
        <div>{children}</div>
        </div>

  )
}
